"""Quant metrics layer: a feature library, small statistical models, and a
scored report. Everything here is deterministic pure Python over cached
candles — same input, same output — per the cardinal rule in context.md.
"""
